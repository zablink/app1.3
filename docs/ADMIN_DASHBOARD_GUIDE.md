# Admin Dashboard Guide

## Overview
หน้า Admin Dashboard ให้ภาพรวมและสถิติการใช้งานของระบบทั้งหมด

## Features

### 1. Dashboard หลัก (Overview Tab)
แสดงสถิติหลักและภาพรวมของระบบ:
- **Key Metrics**: จำนวนร้านค้า, ผู้ใช้งาน, Creators, และ Subscriptions
- **Status Overview**: สถานะร้านค้าและ Creators (อนุมัติ, รออนุมัติ, ปฏิเสธ)
- **Activity Stats**: สถิติแคมเปญ, รีวิว, และ Subscriptions

### 2. Shops Tab
รายงานเกี่ยวกับร้านค้า:
- จำนวนร้านค้าทั้งหมด
- ร้านค้าที่รออนุมัติ
- ร้านค้าที่อนุมัติแล้ว
- สถิติร้านค้าตามสถานะ (แสดงเป็น Progress Bar)

### 3. Users Tab
รายงานเกี่ยวกับผู้ใช้งาน:
- จำนวนผู้ใช้งานทั้งหมด
- จำนวน Creators
- จำนวนเจ้าของร้านค้า
- กราฟการเติบโตของผู้ใช้งาน (coming soon)

### 4. Revenue Tab
รายงานรายได้จาก Subscriptions:
- Subscriptions ที่ Active
- Subscriptions ที่หมดอายุ
- รายได้โดยประมาณ
- กราฟรายได้รายเดือน (coming soon)
- กราฟการกระจายตัวตาม Package (coming soon)

## API Endpoints

### GET `/api/admin/dashboard`
ดึงข้อมูลสถิติทั้งหมดสำหรับ Dashboard

**Response:**
```json
{
  "shops": {
    "total": 0,
    "pending": 0,
    "approved": 0,
    "rejected": 0
  },
  "creators": {
    "total": 0,
    "pending": 0,
    "approved": 0
  },
  "campaigns": {
    "total": 0,
    "active": 0
  },
  "users": {
    "total": 0
  },
  "subscriptions": {
    "active": 0,
    "expired": 0,
    "revenue": 0
  },
  "reviews": {
    "total": 0,
    "avgRating": 0
  }
}
```

## Components

### Dashboard Page Components
ใช้ใน `/src/app/admin/dashboard/page.tsx`:
- `TabButton`: ปุ่มสลับ Tab
- `MetricCard`: การ์ดแสดงตัวเลขสถิติพร้อม Trend
- `StatusRow`: แถวแสดงสถานะพร้อม Progress Bar
- `ActivityCard`: การ์ดแสดงกิจกรรม
- `StatsCard`: การ์ดแสดงสถิติทั่วไป
- `ProgressBar`: แถบแสดงความคืบหน้า

### Reusable Components
อยู่ใน `/src/components/admin/DashboardComponents.tsx`:
- สามารถนำไปใช้ในหน้าอื่นๆ ได้
- Styled ด้วย Tailwind CSS
- รองรับ TypeScript

## Access Control
- ต้องล็อกอินด้วย role `ADMIN` เท่านั้น
- ใช้ `requireAdmin()` middleware ในการตรวจสอบสิทธิ์
- Redirect ไปหน้าแรกถ้าไม่มีสิทธิ์

## Usage

1. เข้าหน้า Admin Dashboard:
   ```
   /admin/dashboard
   ```

2. หรือจากหน้า Admin หลัก:
   ```
   /admin → คลิก "Dashboard & Analytics"
   ```

## Future Enhancements
- 📊 กราฟแสดงการเติบโตของผู้ใช้งาน
- 💰 กราฟรายได้รายเดือน
- 📈 กราฟการกระจายตัวของ Subscription Packages
- 🔔 Recent Activities แบบ Real-time
- 📅 ตัวกรองตามช่วงเวลา (Date Range Picker)
- 📊 Export รายงานเป็น CSV/Excel
- 📱 Responsive design สำหรับมือถือ

## Technical Details

### State Management
- ใช้ React Hooks (`useState`, `useEffect`)
- Fetch ข้อมูลจาก API เมื่อโหลดหน้า
- มีปุ่ม Refresh เพื่ออัพเดทข้อมูล

### Styling
- Tailwind CSS
- Responsive Grid Layout
- Hover Effects & Transitions
- Color Coding ตามประเภทข้อมูล

### Icons
- Lucide React Icons
- สีและไอคอนสอดคล้องกับประเภทข้อมูล

## Notes
- ข้อมูลบางส่วนอาจเป็น placeholder (--) ถ้ายังไม่มีข้อมูล
- กราฟและรายงานเพิ่มเติมจะพัฒนาในอนาคต
- สามารถขยายฟังก์ชันได้ตามต้องการ
