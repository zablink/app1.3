# 📋 แผนการ Implement Dashboard และ Features

## ⚠️ เรื่อง Migration

**คำถาม:** Migration จากไหนไปไหน?  
**คำตอบ:** เนื่องจากเป็นโปรเจคเริ่มต้นจาก 0

**วิธีแก้:**
1. **ใช้ `prisma db push`** (แนะนำสำหรับ development)
   ```bash
   npx prisma db push
   ```
   - จะสร้าง/อัปเดต tables ตาม schema.prisma โดยอัตโนมัติ
   - ไม่ต้องมี migration files

2. **หรือสร้าง Initial Migration** (ถ้าต้องการ version control)
   ```bash
   npx prisma migrate dev --name init
   ```
   - จะสร้าง migration ที่สร้าง tables ทั้งหมดพร้อม OG fields

**Migration file ที่สร้างไว้** (`20250101_add_og_campaign_fields`) สามารถลบได้ถ้าใช้วิธีที่ 1

---

## 🏪 1. Shop Dashboard - หน้าที่ต้องสร้าง

### 1.1 Dashboard หลัก (`/dashboard/shop`)
**สถานะ:** มีอยู่แล้ว แต่ต้องเพิ่ม:
- ✅ Analytics (มีอยู่แล้ว)
- ➕ **Token Wallet Section**
- ➕ **Subscription Plan Section** (มีบางส่วนแล้ว)
- ➕ **Quick Actions** (เพิ่มโฆษณา, จ้าง Creator)

### 1.2 หน้าโฆษณาร้าน (`/dashboard/shop/ads`)
**ต้องสร้างใหม่**

**Features:**
- แสดงโฆษณาที่กำลังใช้งาน
- สร้างโฆษณาใหม่
- เลือกระดับ: ตำบล (50 tokens/วัน), อำเภอ (200), จังหวัด (500), ภูมิภาค (1,500), ทั่วประเทศ (3,000)
- เลือกระยะเวลา
- คำนวณราคา (รวมส่วนลด OG)
- ประวัติโฆษณา
- สถิติ (views, clicks)

**API ที่ต้องสร้าง:**
- `GET /api/ads?shopId=xxx` - ดึงโฆษณาของร้าน
- `GET /api/ads/stats?shopId=xxx` - สถิติโฆษณา

### 1.3 หน้าจ้าง Creator (`/dashboard/shop/campaigns`)
**สถานะ:** มีบางส่วน (`/dashboard/shop/select-reviewers`)

**Features:**
- สร้าง Campaign ใหม่
- กำหนด budget (tokens)
- เลือก Creator
- ดู Campaigns ทั้งหมด
- อนุมัติ/ปฏิเสธผลงาน

**API ที่ต้องสร้าง:**
- `POST /api/campaigns` - สร้าง Campaign
- `GET /api/campaigns?shopId=xxx` - ดึง Campaigns ของร้าน

### 1.4 หน้าดูยอด (`/dashboard/shop/reports`)
**ต้องสร้างใหม่**

**Features:**
- สรุป Token (ซื้อ, ใช้, คงเหลือ)
- สรุป Campaign (จ่าย Creator)
- สรุปโฆษณา
- Export report

---

## 👤 2. Creator Dashboard - หน้าที่ต้องสร้าง

### 2.1 Dashboard หลัก (`/dashboard/creator`)
**สถานะ:** มีอยู่แล้ว แต่ต้องเพิ่ม:
- ✅ รายได้ (มีอยู่แล้ว)
- ➕ **แสดงระยะทาง** จาก Creator ไปร้าน
- ➕ **แสดง Campaigns ที่รอรับ**

### 2.2 หน้าเบิกเงิน (`/dashboard/creator/withdraw`)
**ต้องสร้างใหม่**

**Features:**
- แบบฟอร์มเบิกเงิน
- ข้อมูลบัญชีธนาคาร
- ประวัติการเบิกเงิน
- สถานะการเบิกเงิน

**API ที่ต้องสร้าง:**
- `POST /api/creator/withdraw` - ขอเบิกเงิน
- `GET /api/creator/withdrawals` - ประวัติการเบิกเงิน

### 2.3 หน้า Campaigns ที่รอรับ (`/dashboard/creator/available-campaigns`)
**ต้องสร้างใหม่**

**Features:**
- แสดง Campaigns ที่เปิดรับ Creator
- แสดงระยะทาง
- แสดง budget
- รับงาน

---

## 💳 3. Payment Pages

### 3.1 หน้าชำระเงิน Subscription (`/payment/subscription`)
**ต้องสร้างใหม่**

**Features:**
- แสดงรายละเอียด Plan
- ชำระเงินผ่าน Omise
- แสดงสถานะ

### 3.2 หน้าซื้อ Token (`/payment/tokens`)
**ต้องสร้างใหม่**

**Features:**
- เลือกจำนวน Token
- แสดงราคา
- ชำระเงินผ่าน Omise

---

## 📍 4. Location System

### 4.1 ตรวจสอบตำบลของร้าน
**สถานะ:** มีระบบอยู่แล้ว แต่ต้องตรวจสอบความแม่นยำ

**สิ่งที่ต้องทำ:**
- ตรวจสอบ Shop registration ให้บันทึก `tambon_id`, `amphure_id`, `province_id`
- ใช้ `getLocationFromCoordinates()` จาก `location-service.ts`
- อัปเดต Shop เมื่อ register/edit

### 4.2 การแสดงร้านใกล้เคียง
**สถานะ:** มีอยู่แล้วใน `/api/shops`
- หน้าแรก: แสดงร้านใกล้ที่สุด ✅
- หมวดหมู่: ต้องตรวจสอบว่ามีการเรียงตามระยะทางหรือไม่

---

## 📢 5. Ad Display Logic

### 5.1 การแสดงโฆษณาตามตำบล
**Logic:**
1. ดูตำบลของผู้ใช้ (จาก geolocation หรือ manual)
2. แสดงโฆษณาที่:
   - `target_area_type = 'tambon'` AND `target_area_id = user_tambon_id`
   - หรือ `target_area_type = 'amphure'` AND `target_area_id = user_amphure_id`
   - หรือ `target_area_type = 'province'` AND `target_area_id = user_province_id`
   - หรือ `target_area_type = 'nationwide'`
3. เรียงตาม `priority` (สูงกว่า = แสดงก่อน)

---

## 📊 สรุปสิ่งที่ต้องทำ (Priority Order)

### Phase 1: Core Features (ทำก่อน)
1. ✅ แก้ไข Migration (ใช้ db push)
2. ➕ Shop Dashboard - Token Wallet Section
3. ➕ Shop Dashboard - Ads Page
4. ➕ Shop Dashboard - Campaigns Page
5. ➕ Creator Dashboard - Withdraw Page
6. ➕ Payment Pages (Subscription, Token)

### Phase 2: Enhanced Features
7. ➕ Creator Dashboard - Available Campaigns
8. ➕ Shop Dashboard - Reports Page
9. ➕ Location Validation

### Phase 3: Nice to Have
10. ➕ Export Reports
11. ➕ Advanced Analytics

---

**Last Updated:** 2025-01-XX
