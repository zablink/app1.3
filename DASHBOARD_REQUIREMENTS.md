# 📋 สรุปความต้องการ Dashboard และ Features

## ⚠️ เรื่อง Migration

**คำถาม:** Migration จากไหนไปไหน?  
**คำตอบ:** เนื่องจากเป็นโปรเจคเริ่มต้นจาก 0 ยังไม่มี data ต้นทาง

**วิธีแก้:**
- ใช้ `prisma db push` แทน migration (สำหรับ development)
- หรือสร้าง initial migration ที่สร้าง tables ทั้งหมดพร้อม OG fields
- Migration file ที่สร้างไว้ (`20250101_add_og_campaign_fields`) จะทำงานได้เมื่อรัน `prisma migrate dev` หรือ `prisma db push`

---

## 🏪 1. Shop Dashboard - หน้าที่ต้องเพิ่ม/ปรับปรุง

### 1.1 Dashboard หลัก (`/dashboard/shop`)
**สถานะปัจจุบัน:** มีอยู่แล้ว แต่ยังไม่ครบ

**สิ่งที่ต้องเพิ่ม:**

#### A. Token Wallet Section
- แสดงยอด Token ปัจจุบัน
- แสดง Token ที่จะหมดอายุเร็วๆ นี้ (30 วัน)
- ประวัติการซื้อ Token
- ประวัติการใช้ Token
- ปุ่มซื้อ Token เพิ่ม

#### B. Subscription Plan Section
- แสดง Plan ปัจจุบัน
- แสดงวันที่หมดอายุ
- แสดง Features ที่ได้
- ปุ่มเปลี่ยน Plan / Upgrade
- แสดง OG status (ถ้าเป็น OG)

#### C. Reports & Analytics
- สถิติการเข้าชม (มีอยู่แล้ว)
- สถิติการคลิก
- สถิติการ Bookmark
- สถิติการรีวิว
- กราฟแสดงแนวโน้ม
- Export report

#### D. Quick Actions
- ✅ แก้ไขข้อมูลร้าน (มีอยู่แล้ว)
- ➕ **โฆษณาร้าน** (ต้องเพิ่ม)
- ➕ **จ้าง Creator** (ต้องเพิ่ม)
- ➕ **ดู Campaigns** (ต้องเพิ่ม)

---

### 1.2 หน้าโฆษณาร้าน (`/dashboard/shop/ads`)
**สถานะ:** ยังไม่มี

**Features ที่ต้องมี:**
- แสดงโฆษณาที่กำลังใช้งาน
- สร้างโฆษณาใหม่
- เลือกระดับการโฆษณา:
  - ตำบล (SUBDISTRICT) - 50 tokens/วัน
  - อำเภอ (DISTRICT) - 200 tokens/วัน
  - จังหวัด (PROVINCE) - 500 tokens/วัน
  - ภูมิภาค (REGION) - 1,500 tokens/วัน
  - ทั่วประเทศ (NATIONWIDE) - 3,000 tokens/วัน
- เลือกระยะเวลา (วัน)
- คำนวณราคา Token (รวมส่วนลด OG ถ้ามี)
- แสดงประวัติโฆษณา
- แสดงสถิติ (views, clicks)

**API ที่ต้องใช้:**
- `POST /api/ads/purchase` - มีอยู่แล้ว
- `GET /api/ads/stats?shopId=xxx` - ต้องสร้าง
- `GET /api/ads?shopId=xxx` - ต้องสร้าง

---

### 1.3 หน้าจ้าง Creator (`/dashboard/shop/campaigns`)
**สถานะ:** มีบางส่วนแล้ว (`/dashboard/shop/select-reviewers`)

**Features ที่ต้องมี:**
- สร้าง Campaign ใหม่
- กำหนด budget (tokens)
- เลือก Creator (มีหน้าเลือกอยู่แล้ว)
- ดู Campaign ที่กำลังดำเนินการ
- ดู Campaign ที่เสร็จแล้ว
- อนุมัติ/ปฏิเสธผลงาน Creator
- ดูรายละเอียดการจ่ายเงิน

**API ที่ต้องใช้:**
- `POST /api/campaigns` - ต้องสร้าง
- `GET /api/campaigns?shopId=xxx` - ต้องสร้าง
- `POST /api/campaign-jobs/[id]/complete` - มีอยู่แล้ว

---

### 1.4 หน้าดูยอดต่างๆ (`/dashboard/shop/reports`)
**สถานะ:** ยังไม่มี

**Features ที่ต้องมี:**
- สรุปยอด Token (ซื้อ, ใช้, คงเหลือ)
- สรุปยอด Campaign (จ่าย Creator)
- สรุปยอดโฆษณา
- สรุปยอด Subscription
- Export เป็น PDF/Excel

---

## 👤 2. Creator Dashboard - หน้าที่ต้องเพิ่ม/ปรับปรุง

### 2.1 Dashboard หลัก (`/dashboard/creator`)
**สถานะปัจจุบัน:** มีอยู่แล้ว แต่ยังไม่ครบ

**สิ่งที่ต้องเพิ่ม:**

#### A. งานที่ได้รับ
- ✅ แสดงงาน (มีอยู่แล้ว)
- ➕ **แสดงระยะทาง** (ต้องเพิ่ม)
- ➕ **แสดงสถานะ** (มีอยู่แล้ว)
- ➕ **แสดงรายละเอียดร้าน** (ต้องเพิ่ม)

#### B. คนที่สนใจ / Campaigns ที่เปิด
- ➕ **แสดง Campaigns ที่รอ Creator รับ** (ต้องเพิ่ม)
- ➕ **แสดงระยะทางจาก Creator** (ต้องเพิ่ม)
- ➕ **แสดง budget** (ต้องเพิ่ม)

#### C. รายได้
- ✅ แสดงรายได้ทั้งหมด (มีอยู่แล้ว)
- ✅ แสดงยอดที่ถอนได้ (มีอยู่แล้ว)
- ➕ **แสดงรายละเอียดรายได้** (ต้องเพิ่ม)
- ➕ **แสดง Token ที่ได้รับ** (ต้องเพิ่ม - แต่ Creator ได้เงินเป็นบาท ไม่ใช่ Token)

#### D. หน้าเบิกเงิน (`/dashboard/creator/withdraw`)
**สถานะ:** ยังไม่มี

**Features ที่ต้องมี:**
- แบบฟอร์มเบิกเงิน
- ระบุจำนวนเงิน
- ระบุข้อมูลบัญชีธนาคาร
- แสดงประวัติการเบิกเงิน
- แสดงสถานะการเบิกเงิน

**API ที่ต้องใช้:**
- `POST /api/creator/withdraw` - ต้องสร้าง
- `GET /api/creator/withdrawals` - ต้องสร้าง

---

## 💳 3. Payment Page (Omise)

### 3.1 หน้าชำระเงิน Subscription
**สถานะ:** ยังไม่มี

**Features:**
- แสดงรายละเอียด Plan
- แสดงราคา
- ชำระเงินผ่าน Omise
- แสดงสถานะการชำระเงิน

### 3.2 หน้าซื้อ Token
**สถานะ:** ยังไม่มี

**Features:**
- เลือกจำนวน Token
- แสดงราคา
- ชำระเงินผ่าน Omise
- แสดง Token ที่ได้รับ

**API ที่ต้องใช้:**
- `POST /api/shops/[shopId]/tokens` - มีอยู่แล้ว (สร้าง charge)
- `POST /api/payment/omise/webhook` - มีอยู่แล้ว

---

## 📍 4. Location System - ระบบตำบล

### 4.1 การตรวจสอบตำบลของร้าน
**สถานะ:** มีระบบ geom อยู่แล้ว แต่ต้องตรวจสอบความแม่นยำ

**สิ่งที่ต้องทำ:**
- ตรวจสอบว่า Shop มี `tambon_id`, `amphure_id`, `province_id` หรือไม่
- ถ้าไม่มี → ใช้ `location` (geometry) + PostGIS หา
- ตรวจสอบความแม่นยำของข้อมูล

**API ที่ต้องใช้:**
- `POST /api/location/reverse-geocode` - มีอยู่แล้ว
- ต้องอัปเดต Shop registration ให้บันทึก tambon_id, amphure_id, province_id

### 4.2 การแสดงร้านใกล้เคียง
**สถานะ:** มีอยู่แล้วใน `/api/shops` แต่ต้องตรวจสอบ

**สิ่งที่ต้องทำ:**
- หน้าแรก: แสดงร้านใกล้ที่สุด (มีอยู่แล้ว)
- หมวดหมู่: แสดงร้านใกล้ๆ (ต้องตรวจสอบ)

---

## 📢 5. Ad System - ระบบโฆษณา

### 5.1 การลงโฆษณา
**สถานะ:** API มีอยู่แล้ว แต่ยังไม่มี UI

**Features:**
- เลือกระดับ:
  - ตำบล (SUBDISTRICT)
  - อำเภอ (DISTRICT)
  - จังหวัด (PROVINCE)
  - ภูมิภาค (REGION)
  - ทั่วประเทศ (NATIONWIDE)
- เลือกระยะเวลา (วัน)
- คำนวณราคา Token
- จ่ายด้วย Token

### 5.2 การแสดงโฆษณา
**Logic:**
- ดูตำบลของผู้ใช้ (จาก geolocation หรือ manual selection)
- แสดงโฆษณาที่ร้านลงไว้ในตำบลนั้น
- แสดงตาม placement (hero, sidebar, category_top, etc.)

---

## 📊 สรุปสิ่งที่ต้องทำ

### High Priority
1. ✅ **Migration** - แก้ไขให้เหมาะกับโปรเจคใหม่ (ใช้ db push หรือ initial migration)
2. ➕ **Shop Dashboard - Token Section** - แสดง Token wallet
3. ➕ **Shop Dashboard - Ads Page** - หน้าลงโฆษณา
4. ➕ **Shop Dashboard - Campaigns Page** - หน้าจ้าง Creator
5. ➕ **Creator Dashboard - Withdraw Page** - หน้าเบิกเงิน
6. ➕ **Payment Pages** - หน้าชำระเงิน (Subscription, Token)

### Medium Priority
7. ➕ **Shop Dashboard - Reports Page** - หน้าดูยอดต่างๆ
8. ➕ **Creator Dashboard - Available Campaigns** - แสดง Campaigns ที่รอรับ
9. ➕ **Location Validation** - ตรวจสอบความแม่นยำของตำบล

### Low Priority
10. ➕ **Export Reports** - Export PDF/Excel
11. ➕ **Ad Statistics** - สถิติโฆษณา

---

**Last Updated:** 2025-01-XX
