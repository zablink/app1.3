# 📋 สรุปการ Implement Dashboard และ Features

## ✅ สิ่งที่สร้างเสร็จแล้ว

### 1. Shop Dashboard

#### 1.1 Token Wallet Section
- **ไฟล์:** `src/app/dashboard/shop/page.tsx` (เพิ่ม TokenWalletSection component)
- **Features:**
  - แสดงยอด Token คงเหลือ
  - แสดง Token ที่จะหมดอายุเร็วๆ นี้ (30 วัน)
  - แสดงจำนวน Batches
  - ปุ่มซื้อ Token
  - ลิงก์ไปดูรายงาน

#### 1.2 หน้าโฆษณาร้าน
- **ไฟล์:** `src/app/dashboard/shop/ads/page.tsx`
- **Features:**
  - แสดงโฆษณาที่กำลังใช้งาน
  - สร้างโฆษณาใหม่
  - เลือกระดับ: ตำบล (50 tokens/วัน), อำเภอ (200), จังหวัด (500), ภูมิภาค (1,500), ทั่วประเทศ (3,000)
  - เลือกระยะเวลา (วัน)
  - คำนวณราคา Token อัตโนมัติ
  - แสดงสถานะโฆษณา (รอเริ่ม, กำลังใช้งาน, หมดอายุ)

#### 1.3 หน้าจ้าง Creator
- **ไฟล์:** `src/app/dashboard/shop/campaigns/page.tsx`
- **Features:**
  - สร้าง Campaign ใหม่
  - กำหนด budget (tokens)
  - กำหนดจำนวน Creator ที่ต้องการ
  - กำหนดวันที่เริ่ม-สิ้นสุด
  - ดู Campaigns ทั้งหมด
  - แสดงสถานะและจำนวน Creator ที่รับงานแล้ว
  - ลิงก์ไปเลือก Creator

#### 1.4 หน้าดูยอด
- **ไฟล์:** `src/app/dashboard/shop/reports/page.tsx`
- **Features:**
  - สรุป Token (ซื้อ, ใช้, คงเหลือ)
  - สรุป Campaign (Budget, จ่าย Creator, คงเหลือ)
  - สรุปโฆษณา (จำนวน, ใช้ Token)
  - Export PDF (ปุ่มพร้อม แต่ยังไม่ได้ implement)

### 2. Creator Dashboard

#### 2.1 หน้าเบิกเงิน
- **ไฟล์:** `src/app/dashboard/creator/withdraw/page.tsx`
- **Features:**
  - แสดงยอดที่ถอนได้
  - แบบฟอร์มเบิกเงิน (จำนวนเงิน, ธนาคาร, เลขบัญชี, ชื่อบัญชี)
  - ตรวจสอบจำนวนเงินขั้นต่ำ (100 บาท)
  - ประวัติการเบิกเงิน
  - แสดงสถานะ (รอตรวจสอบ, กำลังดำเนินการ, เสร็จสิ้น, ปฏิเสธ)

#### 2.2 หน้า Campaigns ที่รอรับ
- **ไฟล์:** `src/app/dashboard/creator/available-campaigns/page.tsx`
- **Features:**
  - แสดง Campaigns ที่เปิดรับ Creator
  - แสดงระยะทางจาก Creator ไปร้าน (ถ้ามีข้อมูล location)
  - แสดง budget และจำนวน Creator ที่ต้องการ
  - แสดงวันที่เริ่ม-สิ้นสุด
  - ปุ่มรับงาน

### 3. Payment Pages

#### 3.1 หน้าชำระเงิน Subscription
- **ไฟล์:** `src/app/payment/subscription/page.tsx`
- **Features:**
  - แสดงรายละเอียด Plan
  - แสดง Features ที่ได้รับ
  - แสดงราคา
  - ปุ่มชำระเงินผ่าน Omise

#### 3.2 หน้าซื้อ Token
- **ไฟล์:** `src/app/payment/tokens/page.tsx`
- **Features:**
  - แสดง Token Wallet ปัจจุบัน
  - เลือกแพ็กเกจ Token (100, 500, 1000, 5000, 10000 tokens)
  - แสดงโบนัส (ถ้ามี)
  - แสดงราคา
  - ปุ่มชำระเงินผ่าน Omise

### 4. API Routes

#### 4.1 Shop APIs
- **`GET /api/shops/[shopId]/tokens/wallet`** - ดึงข้อมูล Token Wallet
- **`GET /api/shops/[shopId]/ads`** - ดึงโฆษณาของร้าน
- **`GET /api/shops/[shopId]/ads/stats`** - สถิติโฆษณา

#### 4.2 Campaign APIs
- **`GET /api/campaigns?shopId=xxx`** - ดึง Campaigns ของร้าน
- **`POST /api/campaigns`** - สร้าง Campaign ใหม่

#### 4.3 Creator APIs
- **`GET /api/creator/withdraw`** - ดึงประวัติการเบิกเงิน
- **`POST /api/creator/withdraw`** - ขอเบิกเงิน
- **`GET /api/creator/available-campaigns`** - ดึง Campaigns ที่เปิดรับ

---

## 📝 หมายเหตุ

### Models ที่ต้องมีใน Database
- `TokenWallet` - มีอยู่แล้ว
- `TokenPurchase` - ต้องมี (ใช้ในโค้ด)
- `TokenUsage` - ต้องมี (ใช้ในโค้ด)
- `AdPurchase` - ต้องมี (ใช้ในโค้ด)
- `campaigns` - มีอยู่แล้ว
- `campaign_jobs` - มีอยู่แล้ว
- `withdrawals` - มีอยู่แล้ว
- `earnings` - มีอยู่แล้ว

### การใช้งาน
1. **Setup Database:**
   ```bash
   npx prisma db push
   npx prisma generate
   ```

2. **ทดสอบ:**
   - เข้า `/dashboard/shop` เพื่อดู Token Wallet
   - เข้า `/dashboard/shop/ads` เพื่อลงโฆษณา
   - เข้า `/dashboard/shop/campaigns` เพื่อจ้าง Creator
   - เข้า `/dashboard/creator/withdraw` เพื่อเบิกเงิน
   - เข้า `/dashboard/creator/available-campaigns` เพื่อดูงานที่เปิดรับ

### TODO (ยังไม่ได้ทำ)
- [ ] Location Validation - ตรวจสอบความแม่นยำของตำบล
- [ ] Export Reports - สร้าง PDF/Excel
- [ ] Ad Statistics - เพิ่ม views/clicks จาก AdImpression
- [ ] Campaign Detail Page - หน้าแสดงรายละเอียด Campaign
- [ ] Creator Campaign Detail - หน้าแสดงรายละเอียดงานสำหรับ Creator

---

**Last Updated:** 2025-01-XX
